<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 만약 null로 데이터가 전달 되었다면 빈칸이 아니라 null로 인식하라는 설정.
	     value의 NULL은 반드시 대문자로 기재할 것. 소문자로 기재시 에러 발생  
	<settings>
	<setting name="jdbcTypeForNull" value="NULL"></setting>
	</settings>
	-->
	<properties resource="db.properties" />
	
	<typeAliases>
		<typeAlias type="kr.ac.kopo.vo.MemberVO" alias="memberVO" />
		<typeAlias type="kr.ac.kopo.vo.PaymentVO" alias="paymentVO" />
		<typeAlias type="kr.ac.kopo.vo.SubscriptionVO" alias="subscriptionVO" />
	</typeAliases>

  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
      <!-- 
        <property name="driver" value="oracle.jdbc.driver.OracleDriver" />
        <property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"/>
        <property name="username" value="C##MAILME_USER"/>
        <property name="password" value="mailme_pwd"/>
       -->
       <property name="driver" value="${jdbc.driver}" />
        <property name="url" value="${jdbc.url}" />
        <property name="username" value="${jdbc.user}" />
        <property name="password" value="${jdbc.password}" />
      </dataSource>
    </environment>
  </environments>
  <mappers>
    <mapper resource="kr/ac/mapper/paymentMapper.xml" />
    <mapper resource="kr/ac/mapper/subscriptionMapper.xml" />
  </mappers>
</configuration>